#include "../../inc/so_long.h"

void	display_from_m(char **matrix)
{
/*mlx_new_image(); // -> crea una imagen, crea un puntero a donde se encuentra la imagen, hay que pintar sobre ella
	mlx_put_pixel(); // -> pone un pixel en la imagen
	mlx_image_to_window();
	mlx_loop();
	mlx_init(); */

	
}
